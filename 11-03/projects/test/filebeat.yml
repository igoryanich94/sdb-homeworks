filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /usr/share/input/error.log

#    fields:
#      service: NGINX_filebeat

output.logstash:
#  enabled: true
  hosts: ["logstash:5046"]

